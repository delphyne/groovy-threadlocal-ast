apply(plugin:"groovy")
apply(plugin:"eclipse")

dependencies {
    groovy("org.codehaus.groovy:groovy:1.8.0")
    
    testCompile("junit:junit:4.8.2")
}

repositories {
    mavenLocal()
    mavenCentral()
}